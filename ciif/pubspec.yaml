name: filter_listview_example
version: 1.0.0+1

environment:
  sdk: ">=2.12.0 <3.0.0"

dependencies:
  flutter:
    sdk: flutter
  flutter_localizations:
    sdk: flutter
  pie_chart: ^5.3.0
  material_design_icons_flutter: 5.0.6595

  cupertino_icons: ^1.0.2

  # load books from internet
  http: ^0.13.0

dev_dependencies:
  flutter_test:
    sdk: flutter

flutter:
  uses-material-design: true